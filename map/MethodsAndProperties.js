/* Properties */
size;

/* Methods */
forEach(callbackFn, thisArg); // Executes a function for each key-value pair.
entries(); // Returns an iterator for key-value pairs.
keys(); // Returns an iterator for the keys.
values(); // Returns an iterator for the values.
set(key, value); // Adds/updates a key-value pair.
get(key); // Retrieves the value associated with a key.
has(key); // Checks if a key exists.
delete key; // Removes a key-value pair.
clear(); // Removes all key-value pairs.
